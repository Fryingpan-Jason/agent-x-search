# Contributor entry

Read README.md, docs/prd.md, docs/architecture.md, docs/engineer.md, then docs/build-log.md. Product and architecture changes need maintainer agreement. Follow the host's environment rules without copying machine inventories here.

Public scope: stdio MCP, zero runtime dependencies, explicit OAuth/API-key selection, default x_search and opt-in subscription CLI research. The executable schema is the contract; README/clients document CLI interaction. There are no UI pages or HTTP endpoints, so no separate UX/API document set is needed.

Tests are offline by default. Never infer a new spending allowance from historical evidence. Do not add automatic inference retry, billing fallback, copied credentials, unrelated CLI tools, HTTP daemon, browser requirement, telemetry or global dependency installs. Only official Grok may refresh its own credential file.

Prepare code and package checks locally. GitHub/npm/Registry publication and public messages require explicit approval. Maintain meaningful verification and current handoff in the existing docs; no speculative document trees. Public history must exclude private machine artifacts and credentials.
