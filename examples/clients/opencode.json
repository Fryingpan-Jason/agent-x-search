{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "agent-x-search": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "agent-x-search@0.1.0",
        "serve",
        "--auth",
        "oauth",
        "--model",
        "grok-4.6"
      ],
      "enabled": true,
      "timeout": 300000
    }
  }
}
